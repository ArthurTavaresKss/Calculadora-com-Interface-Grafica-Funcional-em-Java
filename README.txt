====================PROJECT=JAVA=CALCULATOR====================

===============================================================

The .exe file will only work if you recreate it. You will need
to create a new shortcut, find the .jar file, CTRL + X it,
write java -jar before and then CTRL + V. It should work.

O arquivo .exe só funcionará se você recriá-lo. Você precisará
criar um novo atalho, encontrar o arquivo .jar, pressionar CTRL + X,
escrever java -jar antes e depois CTRL + V. Deve funcionar.